fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.5.0
anthropic>=0.28.0
python-dotenv>=1.0.0
# The shared modules (qa.py, clients_config.py, indexer.py) already have their
# own deps in the parent requirements.txt. We list only what THIS backend
# module adds. Parent deps still get installed because you run `py -m pip
# install -r requirements.txt` at the project root normally.
#
# If setting up the backend from scratch on a new machine, install parent deps too:
#   pip install -r ../requirements.txt
#   pip install -r requirements.txt
